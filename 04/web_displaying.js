/**
 * Name: Recipe web page with expandable data and search
 * Completion time: max 7 hours
 * Date: 2016-07-01
 *
 * Task 1
 *
 * Create a file structure for a web page with HTML, CSS and JS file.
 * Load the supplied recipe_db.js file to your page.
 * All your solutions should work using the data accessible from that file.
 *
 *
//CHECK
 *
 *
 * Task 2
 *
 * Design and create a page that displays all recipes from the database dynamically.
 * When the page loads it should be populated with a simplified list of recipe data
 * in a way that makes easy to glance trough the recipes but contains enough
 * information for the user to decide if a recipe is interesting to them or not.
 *
 * It's importand to design this page for clarity, simplicity and ease of use.
 *
 *
 *
 *
 * Task 3
 *
 * Make the recipe entries clickable. When a user clicks on an entry expand the
 * selected item and display all information for that recipe.
 * Close the expanded list item on a second click.
 *
 *
 *
 *
 *
 *
 *
 * Task 4
 *
 * Add a search field to the top of the page so the user will be able to search for
 * a recipe name.
 * Filter the list when a user starts typing. If no recipe matches the entered data
 * display a not found message.
 *
 */
